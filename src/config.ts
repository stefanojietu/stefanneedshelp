/**
 * GOAL CONFIGURATION
 * 
 * Edit 'amountNeeded' and 'amountReceived' directly here or via GOAL.md
 */
export const GOAL_CONFIG = {
  title: "Stefan needs help",
  amountNeeded: 30000,
  amountReceived: 0,
  currency: "$",
};
